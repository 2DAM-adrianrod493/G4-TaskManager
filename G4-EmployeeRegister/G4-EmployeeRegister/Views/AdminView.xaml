<Window x:Class="G4_EmployeeRegister.Views.AdminView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:G4_EmployeeRegister.Views"
      mc:Ignorable="d" 
      Title="AdminView" Height="500" Width="800"
      ResizeMode="NoResize">

    <!-- Fondo -->
    <Window.Background>
        <ImageBrush ImageSource="../Resources/Fondos/fondo.jpg" Stretch="Fill" Opacity="0.5"/>
    </Window.Background>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Parte Izquierda -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="4*"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <Grid Grid.Column="0" Grid.Row="0" VerticalAlignment="Top">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="4*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Logo -->
                <Image Source="/Resources/Iconos/logo.png"
                       Grid.Column="0"
                       Width="40"
                       HorizontalAlignment="Center"
                       Style="{StaticResource EstiloImagen}"/>

                <!-- Nombre Usuario -->
                <TextBlock Text="Adrián Rodríguez Rodríguez"
                           Grid.Column="1" 
                           VerticalAlignment="Center"
                           Style="{StaticResource EstiloTextoTitulo}"/>

                <!-- Botón Salir -->
                <Button x:Name="BotonSalir"
                        Grid.Column="2"
                        HorizontalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="BotonSalir_Click">
                    <Image Source="/Resources/Iconos/salir.png"
                           Width="38"
                           Style="{StaticResource EstiloImagen}"/>
                </Button>

            </Grid>

            <!-- Buscador -->
            <Grid Grid.Column="0" Grid.Row="1" VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="4*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Botón Buscar -->
                <Button x:Name="BotonBuscar"
                        Grid.Column="0"
                        HorizontalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="BotonBuscar_Click">
                    <Image Source="/Resources/Iconos/buscar-usuario.png"
                           Width="35"
                           Style="{StaticResource EstiloImagen}"/>
                </Button>

                <!-- Buscador -->
                <TextBox x:Name="BuscarPorNombreID"
                         Grid.Column="1"
                         HorizontalAlignment="Center"
                         Height="40" Width="320" Opacity="0.2"
                         Style="{StaticResource EstilosTextBox}"/>

                <!-- Botón Filtrar -->
                <Button x:Name="BotonFiltrar"
                        Grid.Column="2"
                        HorizontalAlignment="Center"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="BotonFiltrar_Click">
                    <Image Source="/Resources/Iconos/filtrar.png"
                           Width="30"
                           Style="{StaticResource EstiloImagen}"/>
                </Button>

            </Grid>

            <!-- Lista Usuarios -->
            <Grid Grid.Column="0" Grid.Row="2" VerticalAlignment="Top" Margin="0,15,0,0">
                <StackPanel Grid.Row="2">
                    <DataGrid x:Name="HistorialUsuario"
                              AutoGenerateColumns="False"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              ColumnWidth="*"
                              IsReadOnly="True"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              Style="{StaticResource EstilosDataGrid}">

                        <!-- Alinear Contenido Celdas al Centro -->
                        <DataGrid.CellStyle>
                            <Style TargetType="DataGridCell">
                                <Setter Property="HorizontalContentAlignment" Value="Center" />
                                <Setter Property="VerticalContentAlignment" Value="Center" />
                            </Style>
                        </DataGrid.CellStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID"  Binding="{Binding ID}" />
                            <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" />
                            <DataGridTextColumn Header="Puesto" Binding="{Binding Puesto}" />
                            <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Parte Derecha -->
        <Grid Grid.Column="1">
            <!-- Formulario -->
            <Border Style="{StaticResource EstilosBordesFormulario}" 
                    Margin="0,0,0,0">
                <Grid>
                    <!-- Filas -->
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <!-- Username -->
                        <TextBlock Text="Username" Height="Auto" Style="{StaticResource EstilosSubtitulos}"/>
                        <TextBox x:Name="UsernameTextBox" Height="18" Style="{StaticResource EstilosTextBox}" Margin="0,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1">
                        <!-- Nombre Usuario -->
                        <TextBlock Text="Nombre" Height="Auto" Style="{StaticResource EstilosSubtitulos}"/>
                        <TextBox x:Name="NombreTextBox" Height="18" Style="{StaticResource EstilosTextBox}" Margin="0,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="2">
                        <!-- Apellidos Usuario -->
                        <TextBlock Text="Apellidos" Height="Auto" Style="{StaticResource EstilosSubtitulos}"/>
                        <TextBox x:Name="ApellidosTextBox" Height="18" Style="{StaticResource EstilosTextBox}" Margin="0,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="3">
                        <!-- Email Usuario -->
                        <TextBlock Text="Email" Height="Auto" Style="{StaticResource EstilosSubtitulos}"/>
                        <TextBox x:Name="EmailTextBox" Height="18" Style="{StaticResource EstilosTextBox}" Margin="0,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="4">
                        <!-- Contraseña Usuario -->
                        <TextBlock Text="Contraseña" Height="Auto" Style="{StaticResource EstilosSubtitulos}"/>
                        <TextBox x:Name="ContraseñaTextBox" Height="18" Style="{StaticResource EstilosTextBox}" Margin="0,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="5">
                        <!-- Rol Usuario -->
                        <TextBlock Text="Rol" Height="Auto" Style="{StaticResource EstilosSubtitulos}"/>
                        <TextBox x:Name="RolTextBox"  Height="18" Style="{StaticResource EstilosTextBox}" Margin="0,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Row="6">
                        <!-- Foto Usuario -->
                        <TextBlock Text="Foto" Height="Auto" Style="{StaticResource EstilosSubtitulos}"/>
                        <Button Content="Seleccionar Imagen" Click="SeleccionarImagen_Click" Margin="0,0,0,0" Width="Auto" Height="Auto" VerticalAlignment="Bottom"/>
                    </StackPanel>
    
                    <!-- Botones -->
                    <StackPanel Grid.Row="7" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Button Content="Añadir" Style="{StaticResource EstilosButton}" Width="60" Height="30" Click="ClickAñadir"/>
                        <Separator Width="20" Visibility="Hidden"/>
                        <Button Content="Editar" Style="{StaticResource EstilosButton}" Width="60" Height="30" Click="ClickEditar"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Eliminar" Style="{StaticResource EstilosButton}" Width="60" Height="30" Click="ClickAñadir"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
