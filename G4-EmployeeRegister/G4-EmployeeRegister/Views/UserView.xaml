<Window x:Class="G4_EmployeeRegister.Views.UserView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:G4_EmployeeRegister.Views"
      mc:Ignorable="d" 
      Title="UserView" Height="450" Width="800">

    <!-- Fondo -->
    <Window.Background>
        <ImageBrush ImageSource="../Resources/Fondos/fondo.jpg" Stretch="Fill" Opacity="0.5"/>
    </Window.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Logo -->
            <Image Source="/Resources/Iconos/logo.png" Width="40" Style="{StaticResource EstiloImagen}" Grid.Column="0" Margin="15,0,15,0"/>

            <!-- Nombre Usuario -->
            <TextBlock Text="Adrián Rodríguez Rodríguez" Style="{StaticResource EstiloTextoTitulo}" Grid.Column="1" VerticalAlignment="Center" Margin="15,0,15,0"/>

        </Grid>

        <!-- Botones -->
        <Grid Grid.Row="1" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="Iniciar Jornada" Style="{StaticResource EstilosButton}" Grid.Column="0" Command="{Binding IniciarJornadaCommand}" Width="220" Height="45" Margin="15,0,15,0"/>

            <Button Content="Finalizar Jornada" Style="{StaticResource EstilosButton}" Grid.Column="2" Command="{Binding FinalizarJornadaCommand}" Width="220" Height="45" Margin="15,0,15,0"/>
        </Grid>

        <!-- Historial -->
        <StackPanel Grid.Row="2">

            <DataGrid x:Name="HistorialUsuario"
                      AutoGenerateColumns="False"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ColumnWidth="*"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      Style="{StaticResource EstilosDataGrid}">
                
                <!-- Alinear Contenido Celdas al Centro -->
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="HorizontalContentAlignment" Value="Center" />
                        <Setter Property="VerticalContentAlignment" Value="Center" />
                    </Style>
                </DataGrid.CellStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" />
                    <DataGridTextColumn Header="Entrada" Binding="{Binding Entrada}" />
                    <DataGridTextColumn Header="Salida" Binding="{Binding Salida}" />
                </DataGrid.Columns>
                
            </DataGrid>
            
        </StackPanel>
        
    </Grid>
</Window>
